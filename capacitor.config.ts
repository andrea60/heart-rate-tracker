import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.algeriandrea.heartratetracker',
  appName: 'heart-rate-tracker',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
